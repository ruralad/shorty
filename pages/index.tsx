import type { NextPage } from "next";
import Head from "next/head";
import Image from "next/image";

const Home: NextPage = () => {
  return (
    <div className="w-screen h-screen font-mono text-red-900 flex justify-center items-center">
      <p>on dev!</p>
    </div>
  );
};

export default Home;
